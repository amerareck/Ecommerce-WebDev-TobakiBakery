<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.miniproject.dao.ProductDAO">

	<select id="selectBestProd" resultType="Product" >
		<![CDATA[
			SELECT
				product_id,
				product_name,
				product_price,
				product_datetime
			FROM 
				(SELECT
					product_id,
					product_name,
					product_price,
					product_datetime
				FROM 
					PRODUCT
				ORDER BY product_datetime DESC, product_name)
			WHERE
				rownum <= 8
		]]>
	</select>
	
	<select id="selectProductImage" resultType="Product" parameterType="Product">
		<![CDATA[
			SELECT
				product_id,
				product_usecase,
				image_original_name,
				image_type,
				image_data
			FROM
				PRODUCT_IMAGE
				LEFT OUTER JOIN PRODUCT
				USING(product_id)
			WHERE
				product_id = #{productId}
			AND product_usecase = #{productUsecase}
		]]>
	</select>
</mapper>