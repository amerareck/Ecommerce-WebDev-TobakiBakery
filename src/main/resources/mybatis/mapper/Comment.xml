<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.miniproject.dao.CommentDAO">

	<select id="selectNoticeCommentList" parameterType="int" resultType="Comment">
		<![CDATA[
			SELECT
				comment_id,
				comment_content,
				comment_datetime,
				member_id
			FROM COMMENT
			WHERE notice_id = #{notice_id}
			ORDER BY comment_datetime
		]]>
	</select>
	
	<select id="selectHelpdeskCommentList" parameterType="int" resultType="Comment">
		<![CDATA[
			SELECT
				comment_id,
				comment_content,
				comment_datetime,
				member_id
			FROM COMMENT
			WHERE helpdesk_id = #{helpdesk_id}
			ORDER BY comment_datetime
		]]>
	</select>

</mapper>