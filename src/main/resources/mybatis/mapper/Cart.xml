<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.miniproject.dao.CartDAO">
<select id="selectCartItemsByMemberId" resultType="com.mycompany.miniproject.dto.ProductDTO">
        select p.product_id, p.product_name, p.product_price, c.cart_count
        from product p
        inner join cart c on p.product_id = c.product_id
        where c.member_id = #{memberId}
    </select>
    
    <update id="updateCartCount">
    	update cart
    	set cart_count = #{cartCount}
    	where product_id = #{productId}
  
    </update>
    <delete id="deleteCartItem">
    	delete from cart
    	where product_id = #{productId}
    
    </delete>
    
    <insert id="insertCartItem">
	    INSERT INTO cart (member_id, product_id, cart_count)
	    VALUES (#{memberId}, #{productId}, #{cartCount})
	</insert>
</mapper>